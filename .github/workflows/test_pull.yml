name: Test pull

permissions:
  actions: write 
env:
  SYNCTOKEN: ${{ secrets.SYNCTOKEN }} 
  TOKEN: ${{ secrets.TOKEN }} 
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  DATA_PASS: ${{ secrets.DATA_PASS }}       
  GH_URL: ${{ secrets.GH_URL }} 
  EPASS: ${{ secrets.EPASS }}            
  TARGET_REPO: ${{ secrets.TARGET_REPO }}

on:
  workflow_dispatch:

jobs:
  run-service:
    runs-on: ubuntu-latest

    steps:
      - name: Record Real Start Time
        run: |
          echo "REAL_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        id: login_docker
        continue-on-error: true
        with:
          username: mohicai
          password: ${{ secrets.DOCKERPULL }}  

      - name: Log in to Docker Hub --Retry
        if: steps.login_docker.outcome == 'failure'
        uses: docker/login-action@v3
        continue-on-error: true
        with:
          username: mohicai
          password: ${{ secrets.DOCKERPULL }}  

      - name: Cleanup Disk Space (Free up to 30GB+)
        continue-on-error: true
        run: |
          echo "=== Before Cleanup ==="
          SIZE=$(df -B1 / | awk 'NR==2 {print $2}') 
          if [ "$SIZE" -gt $((100*1024*1024*1024)) ]; then 
            echo "大于100GB" 
          else 
            echo "小于100GB" 
            sudo rm -rf /usr/share/dotnet
            sudo rm -rf /usr/local/lib/android
            sudo rm -rf /opt/ghc
            sudo rm -rf /usr/local/share/boost
            sudo rm -rf "$AGENT_TOOLSDIRECTORY"
            docker system prune -a -f --volumes
            echo "=== After Cleanup ==="
          fi

      - name: Checkout Repo B
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.SYNCTOKEN }}

      - name: Check and Sync Repository
        run: |
          echo "当前运行的仓库是: ${{ github.repository }}"
          if [ "${{ github.repository }}" = "Metamask-ctrl/alist" ]; then
            echo "🛡️ 安全防御：检测到当前正是【主仓库本身】，无需执行同步！"
            exit 0
          fi

          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git remote add upstream https://github.com/Metamask-ctrl/alist.git
          git fetch upstream
          
          LOCAL_COMMIT=$(git rev-parse HEAD)
          UPSTREAM_COMMIT=$(git rev-parse upstream/main)
          
          if [ "$LOCAL_COMMIT" = "$UPSTREAM_COMMIT" ]; then
            echo "☕ 检查完毕：当前已经是最新版本，无需同步。"
            exit 0
          fi

          git checkout main
          git reset --hard upstream/main
          git push origin main --force
          
          HTTP_STATUS=$(curl -s -o response.json -w "%{http_code}" -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $SYNCTOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/mksha8721/abb/dispatches" \
            -d '{"event_type": "mksha_trigger"}')

          if [ "$HTTP_STATUS" -eq 204 ]; then
            echo "🎉【触发成功】目标工作流已启动！"
          else
            echo "❌【触发失败】API 请求返回异常！"
            cat response.json
            exit 1
          fi
          exit 1

      - name: Cache APT Packages
        id: cache-apt
        uses: actions/cache@v4
        with:
          path: /var/cache/apt/archives
          key: ${{ runner.os }}-apt-rclone-fuse3-expect

      - name: Install Dependencies (unzip, rclone, fuse3)
        id: step_deps
        continue-on-error: true
        run: |
          sudo ip addr add 192.168.5.1/24 dev eth0 || true
          cd /opt
          wget "https://download.oracle.com/java/25/latest/jdk-25_linux-x64_bin.tar.gz" -O jdk25.tar.gz && tar -xzf jdk25.tar.gz 2>&1 & 
          sudo apt-get install -y --no-install-recommends expect fuse3          
          sudo curl -fsSL https://rclone.org/install.sh | sudo bash

      - name: Mount s3
        id: step_mount
        continue-on-error: true
        env:
          RCLONECONF: ${{ secrets.RCLONECONF }}
        run: |
          sudo curl -L "$RCLONECONF" -o ./rclone.conf
          if [ -s ./rclone.conf ]; then
            sudo mkdir -p /home/runner/.config/rclone /root/.config/rclone /alist /r3
            sudo cp ./rclone.conf /home/runner/.config/rclone/rclone.conf
            sudo cp ./rclone.conf /root/.config/rclone/rclone.conf
            sudo rclone mount r2:action /r3 --cache-dir /tmp --vfs-cache-mode full --vfs-write-back 10s --vfs-cache-max-size 8G --dir-cache-time 10s --allow-other --daemon
            sleep 3 # 给 mount 留 3 秒挂载响应时间
          else
            echo "错误：加密备份文件下载失败！"
            exit 1
          fi  
          sudo rm -f ./rclone.conf

      - name: Ensure Docker Login Status
        run: |
          sudo mkdir -p /root/.docker
          sudo cp -f ~/.docker/config.json /root/.docker/config.json || true
          sudo docker info | grep -i "username" || true

      # 🌟 修复：改为基于通用 key 进行恢复，并在找不到精确匹配时允许回退恢复（restore-keys）
      - name: Cache Docker Images Tar
        id: cache-docker
        uses: actions/cache@v4
        with:
          path: /tmp/docker-images.tar
          key: docker-img-v1-${{ github.run_id }}
          restore-keys: |
            docker-img-v1-

      - name: Pull or Load Docker Images
        id: step_pull
        continue-on-error: true
        run: |
          export SKIP_DOCKER_INSTALL="true"
          export PANEL_PORT="26627"
          export PANEL_ENTRANCE="ec3ec2eabe"
          export PANEL_USERNAME="32ac1597da"
          export PANEL_PASSWORD="fb107bf8e4"
          
          curl -sSL https://resource.fit2cloud.com/1panel/package/quick_start.sh | sudo -E bash > /tmp/panel.log 2>&1 &
          
          sudo mkdir -p /bbb
          [ -f /r3/bedrock_server-1.26.23.1 ] && sudo cp /r3/bedrock_server-1.26.23.1 /bbb 2>&1 &

          TARGET_DIR="/r3/back"

          if [ ! -d "$TARGET_DIR" ]; then
            echo "❌ 错误: 找不到挂载目录 $TARGET_DIR，跳过 Compose 操作"
            wait
            cat /tmp/panel.log
            exit 0
          fi

          cd "$TARGET_DIR"

          if [ -f /tmp/docker-images.tar ]; then
            echo "⚡ 找到镜像缓存，正在极速加载..."
            sudo docker load -i /tmp/docker-images.tar
          else
            echo "🐢 缓存未命中，开始拉取并生成缓存包..."
            sudo -E docker compose pull
            
            # 安全提取 Compose 关联的镜像列表
            IMAGES=$(sudo -E docker compose config --images 2>/dev/null | sort -u | tr '\n' ' ')
            
            if [ -n "$IMAGES" ]; then
              echo "正在保存以下镜像到缓存文件: $IMAGES"
              sudo docker save $IMAGES -o /tmp/docker-images.tar
              sudo chmod 666 /tmp/docker-images.tar
            else
              echo "⚠️ 未获取到任何 Docker 镜像，跳过 save 步骤"
            fi
          fi

          wait
          cat /tmp/panel.log